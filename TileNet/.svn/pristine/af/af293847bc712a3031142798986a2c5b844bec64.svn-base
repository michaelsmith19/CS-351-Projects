package com.putable.tilenet.Tests;

public class TileNetExceptionHandler implements Thread.UncaughtExceptionHandler{	

	@Override
	public void uncaughtException(Thread t, Throwable e) {
		System.out.println(e.getMessage());
		//TODO can find if exception was port bound error, then attempt to restart on a different port
	}

}
