package com.putable.tilenet.factory;

import com.putable.tilenet.grid.Grid;
import com.putable.tilenet.grid.HomeGrid;
import com.putable.tilenet.keybindset.HomeKeyBindSet;
import com.putable.tilenet.keybindset.KeyBindSet;

public class EmptyMatrixFactory implements MatrixFactory {

	@Override
	public KeyBindSet addKeyBindSet() {
		return new HomeKeyBindSet();
	}

	@Override
	public Grid addGrid() {
		return new HomeGrid();
	}
	
}
